plugins {
    id 'org.jetbrains.kotlin.jvm'
    id 'maven-publish'
}

dependencies {
    implementation(project(":packages:sdk-core"))
    implementation(project(":packages:plugin-iteratively"))
    implementation(project(":packages:plugin-schema-validator"))
    implementation("$jUnitImplementation")
    implementation("$okHttpImplementation")
    implementation("$mockWebServerImplementation")
}

apply from: rootProject.file('gradle/publish-java.gradle')
