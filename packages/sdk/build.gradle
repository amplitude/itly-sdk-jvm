plugins {
    id 'org.jetbrains.kotlin.jvm' version '1.3.72'
    id 'maven-publish'
    id 'signing'
}

//java {
//    withSourcesJar()
//}

repositories {
    jcenter()
    mavenCentral()
    mavenLocal()
}

dependencies {
    // TODO: Remove to seperate module and load seperately in plugins
    implementation(rootProject.findProperty("orgJsonImplementation"))
    testImplementation(":packages:test-fixtures")
}

apply from: rootProject.file('gradle/publish-java.gradle')

//signing {
//    sign publishing.publications.maven
//}

//javadoc {
//    if(JavaVersion.current().isJava9Compatible()) {
//        options.addBooleanOption('html5', true)
//    }
//}

