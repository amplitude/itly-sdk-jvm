//import org.jetbrains.kotlin.gradle.tasks.KotlinCompile

plugins {
    id "application"
    id "java"
}

application {
    mainClassName = "ly.iterative.itly.AppJava"
}

dependencies {
    implementation(project(":packages:sdk"))
//    implementation(project(":packages:plugin-iteratively"))
    implementation 'ly.iterative.itly:plugin-iteratively-jvm:1.0'
    implementation(project(":packages:plugin-iteratively"))
    implementation(project(":packages:plugin-schema-validator"))
}
